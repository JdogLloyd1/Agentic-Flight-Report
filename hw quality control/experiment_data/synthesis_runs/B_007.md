## Executive summary

*   Network health degraded in Central US due to convection `(live)`.
*   Top weather drivers are thunderstorms at DFW `(live)` and 7 convective SIGMETs enroute `(live)`.
*   DFW Ground Stop active until 8:15 PM CDT `(live)`; GS protocols mandate ground holding `(reference)`.
*   82 TFRs nationwide but none directly over terminals `(live)`.

---

## Operational snapshot

**NAS:** DFW Ground Stop active until 8:15 PM CDT `(live)`. ORD, SDF, SFO, MEM under GDP `(live)`. GS protocols require originator approval for release `(reference)`.

**Weather:** DFW reporting thunderstorms with increasing delay trend `(live)`. Enroute hazards include tornadoes and hail up to 2.0 inches `(live)`. Controllers must advise hazardous weather within 150 NM `(reference)`. BOS remains VFR `(live)`.

**Delays/Cancellations:** DFW arrival/departure delays 31-45 minutes `(live)`. ORD averaging 3hr30min GDP `(live)`. System-wide cancellation data unavailable `(live)`. Gate hold procedures apply when delays exceed 15 minutes `(reference)`.

**Other Factors:** 82 active TFRs including VIP and space operations `(live)`. TSA wait times unavailable due to API error `(live)`. Specific DFW/BOS facility text missing from AFD index `(reference)`.

**Hubs:** Thunderstorm activity concentrated in central US affecting multiple hubs `(live)`. ORD max delays reach 10hr59min `(live)`. Specific TMIs for DFW/BOS not detailed in reference excerpts `(reference)`.

---

## Risks / gaps

*   TSA wait times unavailable (API 302 redirect) `(live)`.
*   No system-wide cancellations tool available `(live)`.
*   TFR geometry omitted, preventing precise route intersection analysis `(live)`.
*   Specific SIDs/STARs and facility text missing from reference data `(reference)`.
*   Flight date (2026) is future relative to tool timestamps `(live)`.