## Executive summary

*   Network health is degraded by convective activity triggering a Ground Stop at DFW `(live)`.
*   Top weather drivers include severe thunderstorm warnings near DFW and seven active convective SIGMETs enroute `(live)`.
*   Delay themes center on Thunderstorm GDPs at ORD, MEM, and SDF alongside DFW restrictions `(live)`.
*   TFRs are active but do not directly block the origin or destination airspace `(live)`.

---

## Operational snapshot

**NAS**
A Ground Stop Program is active for DFW until 8:15 PM CDT `(live)`. During a GS, ATC must not release aircraft without originator approval `(reference)`.

**Weather**
DFW reports thunderstorms with severe warnings in Collin, Hunt, and Dallas counties `(live)`. Controllers must advise pilots of hazardous weather impacting operations within 150 NM `(reference)`. Enroute hazards include tornadoes and hail up to 2.0 inches `(live)`.

**Delays/Cancellations**
DFW arrival and departure delays are 31-45 minutes with an increasing trend `(live)`. Gate hold procedures are used when departure delays exceed 15 minutes `(reference)`. No system-wide cancellation tool is available `(live)`.

**Other Factors**
82 active TFRs were retrieved including VIP and space operations `(live)`. TSA wait times for DFW are unavailable due to API redirection `(live)`.

**Hubs**
ORD faces a GDP averaging 3 hours 30 minutes due to thunderstorms `(live)`. MEM and SDF also report thunderstorm-related GDPs `(live)`.

---

## Risks / gaps

*   TSA wait times unavailable due to MyTSA API redirect `(live)`.
*   No dedicated system-wide cancellations tool available `(live)`.
*   TFR geometry omitted preventing precise route intersection analysis `(live)`.
*   Specific TMIs, SIDs, and STARs for the flight date are missing `(reference)`.
*   AFD index lacks actual facility text such as runway configurations `(reference)`.
*   Flight date is in the future relative to tool data timestamps `(live)`.