## Executive summary

*   Network health is degraded due to convective activity impacting central US hubs `(live)`.
*   Top weather drivers are thunderstorms at DFW and seven enroute convective SIGMETs `(live)`.
*   Delay themes center on a DFW Ground Stop and GDPs at ORD, MEM, and SDF `(live)`.
*   TFRs are active nationally but do not directly restrict DFW or BOS terminals `(live)`.
*   Regulatory protocols mandate ground holds for GS/GDP and weather advisories within 150 NM `(reference)`.

---

## Operational snapshot

**NAS:** DFW has a Ground Stop Program active until 8:15 PM CDT `(live)`. Protocols require aircraft meeting criteria to remain on the ground during a GS `(reference)`. Status is otherwise ok outside specific programs `(live)`.

**Weather:** DFW reports thunderstorms with VCTS in the forecast `(live)`. Seven convective SIGMETs are active enroute with hazards including tornadoes and hail `(live)`. Controllers must advise pilots of hazardous weather impacting operations within 150 NM `(reference)`. BOS remains VFR with no active alerts `(live)`.

**Delays/Cancellations:** DFW arrival/departure delays are 31-45 minutes with an increasing trend `(live)`. Gate hold procedures apply when departure delays exceed or are anticipated to exceed 15 minutes `(reference)`. System-wide cancellation data is unavailable via current tools `(live)`.

**Other Factors:** 82 active TFRs exist, including VIP and space operations, but none directly over DFW or BOS `(live)`. TSA wait times for DFW are unavailable due to API redirection `(live)`. Specific departure/arrival procedures for the flight date are missing from reference data `(reference)`.

**Hubs:** ORD, MEM, SDF, and SFO are experiencing GDPs ranging from 49 minutes to 3 hours 30 minutes `(live)`. Thunderstorm activity is concentrated in the central US affecting multiple hubs `(live)`. Time-Based Flow Management may assign metering times to manage arrival flow `(reference)`.

---

## Risks / gaps

*   TSA wait times unavailable due to MyTSA API redirect `(live)`.
*   No dedicated system-wide cancellations tool available `(live)`.
*   TFR geometry omitted preventing precise route intersection analysis `(live)`.
*   Specific TMIs, SIDs, and STARs for DFW/BOS on the flight date are missing `(reference)`.
*   AFD data lacks actual facility text such as runway configurations or ATIS frequencies `(reference)`.