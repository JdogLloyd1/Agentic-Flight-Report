## Executive summary
*   Network health is degraded with active Ground Stop at DFW `(live)` and significant GDPs at ORD `(live)`.
*   Primary weather drivers include 7 convective SIGMETs covering the central US corridor `(live)` and severe thunderstorm warnings in TX `(live)`.
*   Delay themes center on thunderstorm-induced flow restrictions `(live)` while TFRs include VIP and space operation zones `(live)`.
*   Reference protocols indicate Gate Hold procedures apply when departure delays exceed 15 minutes `(reference)`.

---

## Operational snapshot
**NAS**
DFW has a Ground Stop Program active until 8:15 pm CDT `(live)`. Controllers must advise pilots of hazardous weather within 150 NM of their sector `(reference)`. Status is otherwise OK outside specific GDP airports `(live)`.

**Weather**
DFW reports thunderstorms with VFR conditions currently `(live)`. Enroute hazards include tornadoes and hail up to 2.0 inches across multiple SIGMETs `(live)`. BOS remains generally VFR throughout the forecast period `(live)`.

**Delays/Cancellations**
DFW arrival/departure delays are 31-45 minutes with an increasing trend `(live)`. ORD shows an average GDP of 3 hours 30 minutes due to thunderstorms `(live)`. No system-wide cancellation tool is available to assess total network impact `(live)`.

**Other Factors**
82 active TFRs exist including VIP and space operations `(live)`. TSA wait times for DFW are unavailable due to API redirect errors `(live)`. AFD data confirms effective dates but lacks specific facility text like runway configurations `(reference)`.

**Hubs**
Major impacted hubs include ORD, DFW, SFO, MEM, and SDF `(live)`. Thunderstorm activity is concentrated in the central US affecting these nodes `(live)`. Ground Stop protocols require aircraft to remain on the ground without originator approval `(reference)`.

---

## Risks / gaps
*   TSA wait times unavailable due to MyTSA API redirect `(live)`.
*   No dedicated system-wide cancellations tool available `(live)`.
*   TFR geometry omitted preventing precise route intersection analysis `(live)`.
*   Specific TMIs, SIDs, and STARs for DFW/BOS on flight date are missing `(reference)`.
*   Flight date 2026-04-27 is in the future relative to tool data timestamps `(live)`.