## Executive summary

*   Network health is degraded due to convective weather impacting central US hubs `(live)`.
*   Top weather drivers are thunderstorms at DFW and 7 convective SIGMETs enroute `(live)`.
*   Delay themes include a DFW Ground Stop and ORD Ground Delay Program `(live)`.
*   TFR themes show 82 active restrictions but none directly over terminals `(live)`.

---

## Operational snapshot

**NAS:** DFW Ground Stop active until 8:15 PM CDT `(live)`. Status is otherwise OK but monitored `(live)`. Controllers must advise pilots of hazardous weather within 150 NM `(reference)`.

**Weather:** DFW experiencing thunderstorms with severe warnings in nearby counties `(live)`. BOS is VFR with generally clear conditions `(live)`. Enroute hazards include 7 convective SIGMETs with tornadoes and hail `(live)`. Controllers must advise pilots of hazardous weather impacting operations `(reference)`.

**Delays/Cancellations:** DFW arrival/departure delays are 31-45 minutes and increasing `(live)`. ORD has a Ground Delay Program averaging 3 hours 30 minutes `(live)`. Gate hold procedures are used when departure delays exceed 15 minutes `(reference)`. No system-wide cancellation tool is available `(live)`.

**Other Factors:** 82 active TFRs retrieved including VIP and space operations `(live)`. No TFRs identified directly over DFW or BOS `(live)`. TSA wait times for DFW are unavailable due to API error `(live)`.

**Hubs:** Major impacted hubs include ORD, DFW, SFO, MEM, and SDF `(live)`. Thunderstorm activity is concentrated in the central US affecting multiple hubs `(live)`. Specific Traffic Management Initiatives for DFW/BOS on the flight date are lacking `(reference)`.

---

## Risks / gaps

*   TSA wait times unavailable due to MyTSA API redirect `(live)`.
*   No dedicated system-wide cancellations tool available `(live)`.
*   TFR geometry omitted preventing precise route intersection analysis `(live)`.
*   Airport Facility Directory lacks specific facility text like runway configurations `(reference)`.
*   Missing specific departure/arrival procedures for DFW/BOS `(reference)`.