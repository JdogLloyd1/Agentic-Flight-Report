## Executive summary

*   Network health is degraded with a Ground Stop active at KDFW `(live)` and multiple GDPs system-wide `(live)`.
*   Primary weather drivers include thunderstorms at the origin `(live)` and seven active convective SIGMETs enroute `(live)`.
*   Delay themes center on convective weather impacts `(live)`, while TFRs include VIP and space operation restrictions `(live)`.
*   Regulatory protocols mandate ground holds for GS/GDP events `(reference)` and hazardous weather advisories within 150 NM `(reference)`.

---

## Operational snapshot

**NAS:** KDFW has a Ground Stop Program active until 8:15 pm CDT `(live)`. Controllers must not release aircraft during a GS without originator approval `(reference)`. Status is otherwise ok outside specific programs `(live)`.

**Weather:** KDFW reports thunderstorms with VCTS in the TAF `(live)`. Enroute hazards include seven convective SIGMETs with tornadoes and hail `(live)`. Controllers must advise pilots of hazardous weather impacting operations within 150 NM `(reference)`.

**Delays/Cancellations:** KDFW arrival/departure delays are 31-45 minutes and increasing `(live)`. Gate hold procedures are used when departure delays exceed 15 minutes `(reference)`. System-wide cancellation data is unavailable, limited to airport-specific NASSTATUS `(live)`.

**Other Factors:** 82 active TFRs exist, including VIP and space operations, but none directly over terminals `(live)`. TSA wait times for KDFW are unavailable due to API redirect errors `(live)`. AFD data lacks specific facility text like runway configurations `(reference)`.

**Hubs:** ORD, MEM, SDF, and SFO are impacted by GDPs averaging 49 minutes to 3 hours 30 minutes `(live)`. Thunderstorm activity is concentrated in the central US affecting multiple hubs `(live)`. Specific TMIs for KDFW/KBOS on the flight date are missing from context `(reference)`.

---

## Risks / gaps

*   TSA wait times unavailable due to MyTSA API redirect (HTTP 302) `(live)`.
*   No dedicated system-wide cancellations tool available, only airport-specific NASSTATUS data `(live)`.
*   TFR geometry omitted so precise route intersection analysis is not possible `(live)`.
*   Excerpts lack specific Traffic Management Initiatives (TMIs) active for DFW/BOS on the flight date `(reference)`.
*   Specific departure procedures (SIDs) for DFW or arrival procedures (STARs) for BOS are unavailable `(reference)`.
*   AFD index confirms effective dates but does not contain actual facility text `(reference)`.