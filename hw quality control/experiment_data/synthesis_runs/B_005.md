## Executive summary

*   Network health is degraded by DFW Ground Stop and ORD GDP `(live)`.
*   Top weather drivers are thunderstorms at DFW and 7 convective SIGMETs enroute `(live)`.
*   Delay themes include increasing 31-45 minute waits at DFW and 3hr30min averages at ORD `(live)`.
*   TFR themes involve 82 active restrictions though none directly impact terminals `(live)`.
*   GS/GDP protocols mandate ground holding to manage flow and safety `(reference)`.

---

## Operational snapshot

**NAS:** DFW Ground Stop active until 8:15 PM CDT `(live)`. Ground Stop protocols require aircraft to remain on ground without originator approval `(reference)`. Status is otherwise ok outside specific programs `(live)`.

**Weather:** DFW experiencing thunderstorms with VCTS in forecast `(live)`. Seven convective SIGMETs active enroute including tornadoes and hail `(live)`. Controllers must advise pilots of hazardous weather within 150 NM `(reference)`. BOS remains VFR with no active alerts `(live)`.

**Delays/Cancellations:** DFW arrival/departure delays are 31-45 minutes and increasing `(live)`. ORD shows average GDP of 3 hours 30 minutes due to thunderstorms `(live)`. Gate hold procedures apply when departure delays exceed 15 minutes `(reference)`. No system-wide cancellation tool is available `(live)`.

**Other Factors:** 82 active TFRs exist including VIP and space operations `(live)`. No TFRs directly over DFW or BOS identified in current dataset `(live)`. TSA wait times for DFW are unavailable due to API redirect `(live)`. AFD data lacks specific facility text like runway configurations `(reference)`.

**Hubs:** Major impacted hubs include ORD, DFW, SFO, MEM, and SDF `(live)`. Thunderstorm activity is concentrated in the central US affecting multiple hubs `(live)`. Specific TMIs active for DFW/BOS on the flight date are lacking `(reference)`.

---

## Risks / gaps

*   TSA wait times unavailable due to MyTSA API redirect `(live)`.
*   No dedicated system-wide cancellations tool available `(live)`.
*   TFR geometry omitted preventing precise route intersection analysis `(live)`.
*   Specific TMIs, SIDs, and STARs for DFW/BOS missing from reference data `(reference)`.
*   Flight date 2026-04-27 is future-dated relative to tool timestamps `(live)`.