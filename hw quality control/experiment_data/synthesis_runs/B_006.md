## Executive summary

*   Network health is degraded at origin due to active Ground Stop `(live)` and convective SIGMETs along the corridor `(live)`.
*   Top weather drivers include thunderstorms at DFW `(live)` and hazardous weather advisories requiring controller notification within 150 NM `(reference)`.
*   Delay themes center on DFW Ground Stop and ORD GDP `(live)`, while TFR themes involve VIP and space operation restrictions `(live)`.
*   Enroute turbulence reports indicate moderate conditions near both terminals `(live)`.

---

## Operational snapshot

**NAS**
DFW Ground Stop Program is active until 8:15 pm CDT `(live)`. Ground Stop protocols require aircraft meeting specific criteria to remain on the ground `(reference)`.

**Weather**
DFW METAR reports thunderstorms with VCTS in the TAF `(live)`. Seven convective SIGMETs are active enroute involving tornadoes and hail `(live)`. Controllers must advise pilots of hazardous weather impacting operations within 150 NM `(reference)`.

**Delays/Cancellations**
DFW arrival and departure delays are 31-45 minutes with an increasing trend `(live)`. Gate hold procedures are used when departure delays exceed or are anticipated to exceed 15 minutes `(reference)`. No system-wide delay/cancellation tool is available `(live)`.

**Other Factors**
82 active TFRs retrieved including VIP and space operations `(live)`. No TFRs directly over DFW or BOS identified in current dataset `(live)`. TSA wait times for DFW are unavailable due to API redirect `(live)`.

**Hubs**
ORD is experiencing an average GDP of 3hr30min due to thunderstorms `(live)`. Thunderstorm activity is concentrated in the central US affecting multiple hubs `(live)`. Airport Facility Directory data lacks specific facility text for this date `(reference)`.

---

## Risks / gaps

*   TSA wait times unavailable due to MyTSA API redirect `(live)`.
*   No dedicated system-wide cancellations tool available `(live)`.
*   TFR geometry omitted so precise route intersection analysis not possible `(live)`.
*   Excerpts lack specific Traffic Management Initiatives active for DFW/BOS on the flight date `(reference)`.
*   Missing specific departure procedures (SIDs) for DFW or arrival procedures (STARs) for BOS `(reference)`.
*   AFD index confirms effective dates but does not contain actual facility text `(reference)`.